# Note: if SFML libs are already in your typical library path, then you can
# simply run the executable. Otherwise, leaving this out will result in an error
# when trying to load shared libraries.
export LD_LIBRARY_PATH=/home/kevin/Code/SFML/lib:$LD_LIBRARY_PATH && ./bin/sfml
